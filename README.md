# Video Website Hoster (Flask)

This is a small Flask app that serves video files from a local `videos/` folder and provides an HTML5 player that supports fullscreen, seek, and playback speed.

Vibe code final boss

Reads files from a folder named "vidoes", but its been gitignored so the github repo isnt huge